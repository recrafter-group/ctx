name: Check pull request
on:
    - pull_request
jobs:
    build:
        name: Build
        uses: recrafter-group/.github/.github/workflows/build.yml@main
    analyze:
        name: Analyze
        uses: recrafter-group/.github/.github/workflows/analyze.yml@main
        needs:
            - build
    test:
        name: Test
        uses: recrafter-group/.github/.github/workflows/test.yml@main
